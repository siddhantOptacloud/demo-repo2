CREATE TABLE products (category character varying(50), price numeric(10,2), product_id integer, product_name character varying(100), CONSTRAINT products_pkey PRIMARY KEY (category, price, product_id, product_name));
